<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="RoundedButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_selected</item>
        <item name="android:textColor">#FFFFFF</item> <!-- 텍스트 색상 설정 -->
    </style>

    <style name="Theme.AndroidTest" parent="Base.Theme.AndroidTest" />
</resources>